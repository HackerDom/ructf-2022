version: "3.8"

services:
  mongo:
    image: mongo
    restart: always
    ports:
      - "27017:27017"
    env_file:
      - .env
  srv:
    build: ./
    image: snake:0.0.1
    ports:
      - "8080:8080"
    env_file: .env
    depends_on:
      - mongo