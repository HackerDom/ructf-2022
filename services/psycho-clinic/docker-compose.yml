version: '2.2'
services:
  psycho_clinic:

    image: psycho_clinic
    cpus: 1
    mem_limit: 3G
    build:
      context: .
      dockerfile: Dockerfile
    ports:
     - "18323:18323"
    volumes:
      - data:/app/data

    restart: always
          
volumes:
    data: